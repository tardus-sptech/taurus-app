<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@color/white"
    android:orientation="vertical"
    android:padding="20dp"
    android:id="@+id/lgpd_container"
    style="lgpd_containter">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:scrollbars="vertical">

        <TextView
            android:id="@+id/txt_termos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="50dp"
            android:text="@string/lgpd_term"
            android:textAlignment="center"
            android:textColor="@color/black"
            style="lgpd_text"
            />

    </ScrollView>

    <CheckBox
        android:id="@+id/chk_aceitar_termos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:text="Eu li e aceito os termos de uso."
        android:textSize="16sp" />

    <Button
        android:id="@+id/btn_confirm_popup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirmar" />

</LinearLayout>
